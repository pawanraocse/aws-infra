# AWS Deployment Guide

**Version:** 1.0  
**Created:** 2026-01-04

---

## Deployment Options

| Option | Cost | Use Case |
|--------|------|----------|
| **Local** | Free | Development |
| **Budget EC2** | ~$50/month | Hobby, testing |
| **ECS Fargate** | ~$150/month | Production |
| **Kubernetes** | Varies | Multi-cloud, K8s teams |

---

## Quick Start

```bash
# Local Development
docker-compose up

# AWS Budget (~$50/month)
cd terraform/envs/budget && terraform apply

# AWS Production (~$150/month)  
cd terraform/envs/fargate && terraform apply

# Kubernetes (requires existing cluster)
helm install saas-factory ./helm/saas-factory
```

---

## Architecture by Environment

### Local (Docker Compose)
```
┌─────────────────────────────────────────────────────────┐
│  Your Machine                                           │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐   │
│  │ Gateway  │ │ Auth     │ │ Platform │ │ Backend  │   │
│  │ :8080    │ │ :8081    │ │ :8083    │ │ :8082    │   │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘   │
│       └────────────┴────────────┴────────────┘          │
│                         │                               │
│       ┌─────────────────┴─────────────────┐            │
│       ▼                                   ▼            │
│  ┌──────────┐                       ┌──────────┐       │
│  │ Postgres │                       │  Redis   │       │
│  │ :5432    │                       │  :6379   │       │
│  └──────────┘                       └──────────┘       │
└─────────────────────────────────────────────────────────┘
```

### Budget EC2 (~$50/month)
```
┌─────────────────────────────────────────────────────────┐
│  AWS                                                    │
│  ┌────────────────────────────────────────────────────┐│
│  │  EC2 t3.small (runs docker-compose)                ││
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐      ││
│  │  │Gateway │ │ Auth   │ │Platform│ │Backend │      ││
│  │  └────────┘ └────────┘ └────────┘ └────────┘      ││
│  │  ┌────────┐ ┌────────┐                             ││
│  │  │Postgres│ │ Redis  │ ← Containers               ││
│  │  └────────┘ └────────┘                             ││
│  └────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────┘
```

### ECS Fargate Production (~$150/month)
```
┌─────────────────────────────────────────────────────────┐
│  AWS                                                    │
│                                                         │
│  Internet → ALB ─┬─→ Gateway (ECS)                     │
│                  ├─→ Auth (ECS)                        │
│                  ├─→ Platform (ECS)                    │
│                  └─→ Backend (ECS)                     │
│                           │                             │
│            ┌──────────────┴──────────────┐             │
│            ▼                             ▼             │
│     ┌──────────────┐            ┌──────────────┐       │
│     │ RDS Postgres │            │ ElastiCache  │       │
│     │ (Managed)    │            │ (Redis)      │       │
│     └──────────────┘            └──────────────┘       │
└─────────────────────────────────────────────────────────┘
```

### Kubernetes (EKS/GKE/AKS)
```
┌─────────────────────────────────────────────────────────┐
│  Kubernetes Cluster                                     │
│                                                         │
│  Ingress → ┬─→ gateway-pod (deployment)                │
│            ├─→ auth-pod (deployment)                   │
│            ├─→ platform-pod (deployment)               │
│            └─→ backend-pod (deployment)                │
│                        │                                │
│         ┌──────────────┴──────────────┐                │
│         ▼                             ▼                │
│  ┌──────────────┐            ┌──────────────┐          │
│  │ RDS/CloudSQL │            │ Redis/Memcache│         │
│  │ (External)   │            │ (External)    │         │
│  └──────────────┘            └──────────────┘          │
└─────────────────────────────────────────────────────────┘
```

---

## Database Strategy

| Environment | PostgreSQL | Redis |
|-------------|------------|-------|
| Local | Container | Container |
| Budget EC2 | Container | Container |
| ECS Fargate | **RDS** | **ElastiCache** |
| Kubernetes | Managed (RDS/CloudSQL) | Managed |

**Rule:** Production = Use managed services (RDS, ElastiCache)

---

## Terraform Module Status

| Module | Status | Description |
|--------|--------|-------------|
| `cognito-user-pool` | ✅ Done | Cognito with Lambda triggers |
| `ssm-parameters` | ✅ Done | SSM parameter store |
| `vpc` | 🔲 TODO | VPC, subnets, security groups |
| `rds` | 🔲 TODO | PostgreSQL database |
| `elasticache` | 🔲 TODO | Redis cluster |
| `ecr` | 🔲 TODO | Docker image registry |
| `ecs-cluster` | 🔲 TODO | ECS Fargate cluster |
| `ecs-service` | 🔲 TODO | Per-microservice deployment |
| `alb` | 🔲 TODO | Load balancer |

---

## Free Testing Options

| Tool | What It Does | Command |
|------|--------------|---------|
| **Docker Compose** | Local dev | `docker-compose up` |
| **LocalStack** | AWS simulator | `localstack start` |
| **Minikube** | Local K8s | `minikube start` |
| **Kind** | K8s in Docker | `kind create cluster` |

No AWS costs for testing!
