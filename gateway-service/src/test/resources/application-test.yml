spring:
  main:
    allow-bean-definition-overriding: true

# Disable Eureka interactions for tests
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

logging:
  level:
    root: WARN
    com.learning.gateway: DEBUG

security:
  gateway:
    fail-on-missing-tenant: true


